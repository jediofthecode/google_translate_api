require "spec_helper"

describe GoogleTranslateApi do
  it "has a version number" do
    expect(GoogleTranslateApi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
